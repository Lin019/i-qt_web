<!-- ko with: registerExternal -->
<h2>註冊</h2>
<form class="form-horizontal" role="form">
    <h4 data-bind="text: 'Associate your ' + loginProvider() + ' account.'"></h4>
    <hr />
    <p class="text-success">
        您已成功驗證 <strong data-bind="text: loginProvider"></strong>。
            請在下方輸入此網站的使用者名稱，再按一下 [註冊] 按鈕以登入。
    </p>
    <ul class="text-danger" data-bind="foreach: errors">
        <li data-bind="text: $data"></li>
    </ul>
    <div class="form-group">
        <label for="RegisterExternalUserName" class="col-md-2 control-label">使用者名稱</label>
        <div class="col-md-10">
            <input type="text" id="RegisterExternalUserName" class="form-control" data-bind="value: userName, hasFocus: true" />
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <button type="submit" class="btn btn-default" data-bind="click: register, disable: registering">註冊</button>
        </div>
    </div>
</form>
<!-- /ko -->
